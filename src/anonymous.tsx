import * as React from 'react';
import {
  anonymous
} from './anonymous.scss';

const Anonymous = () => (
  <div className={anonymous}>
    <div>{'Anonymous user.'}</div>
  </div>
);

export default Anonymous;
